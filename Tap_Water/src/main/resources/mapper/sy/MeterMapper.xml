<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zhibang.mapper.sy.MetertypeMapper">

    <!--查询全部水表信息-->
    <select id="selectSyMetertype" resultType="syMetertype">
      SELECT * FROM `sy_metertype`
    </select>

</mapper>